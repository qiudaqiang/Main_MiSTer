language: c

matrix:
  include:
    - os: linux
      env: TARGET="linux"

branches:
  only:
    - master

install:
  - sudo apt-get install gcc-arm-linux-gnueabihf
  - sudo apt-get install g++-arm-linux-gnueabihf

script:
  - sh .travis.sh
